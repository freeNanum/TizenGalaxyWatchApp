<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TizenWatchXamlApp1.PromptEx">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Prompt Ex" FontSize="Caption"
                HorizontalOptions="CenterAndExpand" />

            <Button Text="Name Button" x:Name="NameBtn"
                    HorizontalOptions="FillAndExpand"
                    Clicked="OnBtn_Clicked"
                    Margin="0,20,0,0" />
            <Label Text="Result" x:Name="RstName" 
                   HorizontalOptions="CenterAndExpand" />

            <Button Text="Cal. Button" x:Name="CalBtn"
                    HorizontalOptions="FillAndExpand"
                    Clicked="OnBtn_Clicked"
                    Margin="0,20,0,0" />
            <Label Text="Result" x:Name="RstCal" 
                   HorizontalOptions="CenterAndExpand" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>